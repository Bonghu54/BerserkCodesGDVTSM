@page
@model BerserkCodesGDVTSM.Views.Ventaos.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Venta[0].FechaHora_Venta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venta[0].FechaHora_Entrega)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venta[0].Estado_Seguimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Venta[0].Total_Venta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Venta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHora_Venta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaHora_Entrega)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado_Seguimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total_Venta)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id_Venta">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id_Venta">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id_Venta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
