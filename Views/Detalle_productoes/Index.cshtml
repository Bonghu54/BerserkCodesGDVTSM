@page
@model BerserkCodesGDVTSM.Views.Detalle_productoes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_Producto[0].Color)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_Producto[0].Tipo_De_Uso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_Producto[0].Durabilidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_Producto[0].Material)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_Producto[0].Estampado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Detalle_Producto) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Color)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo_De_Uso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Durabilidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Material)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estampado)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id_Detalle_Producto">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id_Detalle_Producto">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id_Detalle_Producto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
