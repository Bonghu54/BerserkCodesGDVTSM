@page
@model BerserkCodesGDVTSM.Views.Personas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Persona[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona[0].Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona[0].Ci)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona[0].Fecha_Nacimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Persona[0].Direccion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Persona) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ci)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha_Nacimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id_Persona">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id_Persona">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id_Persona">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
