@page
@model BerserkCodesGDVTSM.Views.Detalla_de_ventas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_de_venta[0].Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detalle_de_venta[0].Precio_Producto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Detalle_de_venta) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio_Producto)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id_Detalle">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id_Detalle">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id_Detalle">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
